kind: pipeline
type: docker
name: ComputePlatform

platform:
  os: linux
  arch: amd64

steps:
  - name: build
    image: nvidia/cuda:12.6.2-base-ubuntu24.04
    commands:
      - apt update && apt install -y libopencv-dev
      - pip install torch numpy opencv-python matplotlib scipy
